# Python dependencies for the Urban Flow AI Engine layer.
# The C++ engine (engine.cpp) has no Python deps; these are for the
# OSM conversion (osm_to_engine.py / test.py) and the LLM agent (agent.py).
osmnx           # download + model real OSM road networks
scikit-learn    # required by osmnx.distance.nearest_nodes (unprojected graphs)
google-genai    # Gemini tool-calling client (agent.py)
